<h1>Products</h1>

<div id="products">
  <% @products.each do |product| %>
    <div>
      <a href="/products/<%= product.id %>">
        <%= link_to product.name, product %>
      </a>
    </div>
  <% end %>
</div>
